/**
 * Re-export desde src/lib/supabase.ts (ubicación canónica FSD).
 * Mantenemos este archivo para retrocompatibilidad con imports existentes.
 */
export { supabase, USE_SUPABASE, pingSupabase } from "@/lib/supabase";
